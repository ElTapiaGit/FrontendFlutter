name: iuapp
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.1

dependencies:
  carousel_slider: ^5.0.0
  dio: ^5.8.0+1
  flutter:
    sdk: flutter
  flutter_localization: ^0.3.1
  shared_preferences: ^2.5.2
  video_player: ^2.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.15
  retrofit_generator: ^9.1.9
  json_serializable: ^6.9.4

flutter:
  uses-material-design: true

  #aqui se agregar los archivos extraibles para todo el proyecto
  assets:
    - assets/
    - assets/carousel/
